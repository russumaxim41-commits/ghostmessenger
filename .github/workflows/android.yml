name: Build Android
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Fix package.json & app.json
        run: |
          # Очистка зависимостей
          sed -i '/@workspace\/api-client-react/d' package.json
          sed -i 's/"catalog:"/"*"/g' package.json
          sed -i 's/catalog:/*/g' package.json
          
          # Проверка и фикс app.json (добавляем android.package если его нет)
          if ! grep -q "package" app.json; then
            sed -i '/"android": {/a \      "package": "com.russumaxim.ghostmessenger",' app.json
          fi

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile --ignore-scripts

      - name: Build Android APK
        run: |
          npx expo prebuild --platform android --no-install
          cd android
          # Увеличиваем лимиты памяти для Gradle, чтобы не было вылетов
          echo "org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m" >> gradle.properties
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GhostMessenger-Debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
